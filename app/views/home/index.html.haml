%article
  %header
    %b Client Operations

  .grid
    = button_to "Log a Visit", root_path, class: 'contrast', method: :get
    = button_to "Register a New Client", new_client_path, class: 'contrast', method: :get

- if Current.user.admin?
  %article
    %header
      %b Admin Operations (only visible to admins)

    .grid
      = button_to "Create New User", root_path, class: 'contrast', method: :get
